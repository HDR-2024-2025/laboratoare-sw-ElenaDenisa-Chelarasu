<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-primary text-white text-center">
                        <h2>Pagina de profil</h2>
                        <h2>Bine ai venit <%=user.username %>!</h2>
                    </div>
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <% if (user.profileImagePath) { %>
                                <img src="/<%= user.profileImagePath %>" alt="Profil" class="rounded-circle img-thumbnail" style="width: 150px; height: 150px;">
                            <% } else { %>
                                <img src="https://via.placeholder.com/150" alt="Fara imagine de profil" class="rounded-circle img-thumbnail">
                            <% } %>
                        </div>
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th scope="row">Nume utilizator</th>
                                    <td><%= user.username %></td>
                                </tr>
                                <tr>
                                    <th scope="row">Email</th>
                                    <td><%= user.email %></td>
                                </tr>
                                <tr>
                                    <th scope="row">Nume</th>
                                    <td><%= user.lastName %></td>
                                </tr>
                                <tr>
                                    <th scope="row">Prenume</th>
                                    <td><%= user.firstName %></td>
                                </tr>
                                <tr>
                                    <th scope="row">Nume afișat</th>
                                    <td><%= user.displayName %></td>
                                </tr>
                                <tr>
                                    <th scope="row">Website</th>
                                    <td>
                                        <% if (user.websiteUrl) { %>
                                            <a href="<%= user.websiteUrl %>" target="_blank" class="text-decoration-none">
                                                <%= user.websiteUrl %>
                                            </a>
                                        <% } else { %>
                                            Nu există URL
                                        <% } %>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                    <img src="../public/res/doYou.jpg" alt="Ai dori sa cream un atac impreuna?" width="500" height="450">
                    <a href="/trigger-attack" class="btn btn-secondary">Apasa aici daca ai dori sa cream un atac impreuna</a><br/>
                    <a href="/cauta" class="btn btn-secondary">Cauta utilizatori</a>
                    <a href="/private" class="btn btn-secondary">Zona privata</a>
                    <div class="card-footer text-center">
                        <form action="/api/logout" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">Logout</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
